<%= render 'shared/nav' %>

<h1 class="text-center text-dark my-4">Order ID: <%= @order.id %></h1>

<div class="container">
  <div class="card shadow-sm">
  
    <div class="card-body">
      <%= render partial: "order_items/items" %>
    </div>
  </div>

  <div class="d-flex justify-content-between mt-4">
    <h4 class="text-success">Total Amount: ₹<%= @total_amount %></h4>
    <%= link_to "Back to Dashboard", dashboard_index_path, class: "btn btn-primary fw-bold" %>
  </div>
</div>
