<h1 class="text-center text-primary my-4">Order ID: <%= @order.id %></h1>

<div class="container">
  <div class="card shadow-sm">
    <div class="card-header bg-info text-white">
      <h4 class="mb-0">Order Items</h4>
    </div>
    <div class="card-body">
      <%= render partial: "order_items/items" %>
    </div>
  </div>

  <div class="d-flex justify-content-between mt-4">
    <h4 class="text-success">Total Amount: ₹<%= @total_amount %></h4>
    <%= link_to "Back to Dashboard", dashboard_index_path, class: "btn btn-secondary fw-bold" %>
  </div>
</div>
