<nav class="navbar fixed-top flex navbar-expand-lg navbar-dark bg-dark">
<div class="container-fluid">
  <% if current_user %>
    <%= link_to current_user&.restaurant&.name, dashboard_index_path, class: "navbar-brand" %>
  <% end %>
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav ms-auto">
      <li class="nav-item">
        <% if current_user %>
          <%= link_to "Orders", orders_path, class: "nav-link" %>
        <% end %>
      </li>

      <li class="nav-item">
        <% if current_user %>
          <%= link_to "Feedbacks", restaurant_feedbacks_path(current_user&.restaurant), class: "nav-link" %>
        <% end %>
      </li>

      <li class="nav-item">
        <% if current_user %>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-link" %>
        <% end %>
      </li>
    </ul>
  </div>
</div>
</nav>